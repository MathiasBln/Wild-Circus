{% extends 'base.html.twig' %}

{% block title %}Price{% endblock %}

{% block body %}
    <h1>Price</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ price.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ price.title }}</td>
            </tr>
            <tr>
                <th>PriceWeek</th>
                <td>{{ price.priceWeek }}</td>
            </tr>
            <tr>
                <th>PriceWeekend</th>
                <td>{{ price.priceWeekend }}</td>
            </tr>
            <tr>
                <th>PriceHolidays</th>
                <td>{{ price.priceHolidays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('price_index') }}">back to list</a>

    <a href="{{ path('price_edit', {'id': price.id}) }}">edit</a>

    {{ include('price/_delete_form.html.twig') }}
{% endblock %}
